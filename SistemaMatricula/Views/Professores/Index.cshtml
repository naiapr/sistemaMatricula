@model IEnumerable<SistemaMatricula.Models.ViewModel.ProfessorViewModel>

@{
    ViewData["Title"] = "Professores";
}

<h2>@ViewData["Title"]</h2>

<p>
    <a asp-action="Create" class="btn btn-default">Novo Cadastro</a>
</p>

<table class="table table-striped table-hover">
    <thead>
        <tr class="success">

            <th>
                @Html.DisplayNameFor(model => model.Professor.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NomeModalidade)
            </th>
            <th></th>
        </t
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeProfessor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeModalidade)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfessorId">Editar</a>
                <a asp-action="Details" asp-route-id="@item.ProfessorId">Detalhar</a>
                <a asp-action="Delete" asp-route-id="@item.ProfessorId">Deletar</a>
            </td>
        </tr>

        }
    </tbody>

</table>